/**
 * Публичный вход дизайн-системы оболочки.
 * Использование: `import { tokens, Button, Card } from '../designSystem'`.
 */
export { tokens, color, gradient, space, radius, shadow, font, transition, z } from './tokens';
export {
    Button,
    glassFill,
    Card,
    CardHeader,
    CardTitle,
    CardSubtitle,
    CardContent,
    Modal,
    ModalHeader,
    ModalTitle,
    ModalBody,
    ModalFooter,
    ModalClose,
    ConfirmDialog,
    Section,
    SectionHeader,
    SectionTitle,
    Input,
    Select,
    Badge,
    Divider,
    ScrollArea,
    Showcase,
} from './components';
export { default } from './tokens';
